{% import '@ezdesign/parts/content_fields.html.twig' as content_fields %}

<article class="view-type view-type-{{ view_type }} {{ content.contentInfo.contentTypeIdentifier|replace({'_': '-'}) }} vl9">

    {{ content_fields.image(content, location, null, null, 'image-circle') }}

    <header class="article-header">
        <h2 class="title"><a href="{{ path(location) }}">{{ content_fields.title(content) }}</a></h2>
    </header>

</article>
